import React from 'react';
import Layout from '../common/Layout';

function Post({match}) {
    return(
    <>
    <Layout head="Mi fake post" subheading="leer con cuidado"></Layout>
    </>);
}

export default Post;